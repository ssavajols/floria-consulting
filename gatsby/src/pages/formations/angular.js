import React from 'react'
import Formation from '../../components/formation'
import * as formationData from '../../data/angular'

const Landing = props => <Formation data={formationData} />

export default Landing
